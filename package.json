{
  "name": "claude-retry",
  "version": "1.0.0",
  "description": "Envoltorio Windows-nativo de claude (modo no interactivo / -p) que reintenta al alcanzar el limite de uso, con streaming en vivo y deteccion en dos capas.",
  "type": "module",
  "bin": {
    "claude-retry": "claude-retry.mjs"
  },
  "scripts": {
    "start": "node claude-retry.mjs",
    "promote": "npm install -g .",
    "test": "node tests/test-detection.mjs && node tests/test-retry.mjs",
    "test:detection": "node tests/test-detection.mjs",
    "test:retry": "node tests/test-retry.mjs"
  }
}
